apiVersion: v1
kind: Service
metadata:
  name: travel-mcp-server
  namespace: travel-mcp
  labels:
    app: travel-mcp-server
spec:
  selector:
    app: travel-mcp-server
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
